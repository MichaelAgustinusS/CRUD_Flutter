import 'package:NilaiAkm/shared/nav.dart';
import 'package:flutter/material.dart';

class DetailNilai extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Detail Nilai AKM")),
      body: Center(child: Text("Halaman Detail"),),
    );
  }
}